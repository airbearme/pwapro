"use client";

// Temporary fallback component for toast notifications
// Replace with proper toast library when available
const Toaster = () => {
  return null; // No-op component until toast library is installed
};

export { Toaster };
